def send_mail(to, subject, message, bcc) -> str:
    """Sends email `message` to user or users `to` with specified `subject` and optional hidden email(s) `bcc`."""
    print(send_mail.__doc__)
    #pass
send_mail()